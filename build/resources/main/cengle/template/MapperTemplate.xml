<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="${className}Mapper" > 
<!-- Result Map-->
<resultMap id="BaseResultMap" type="${bussPackage}.domain#if($!entityPackage).${entityPackage}#end.$!{className}" >
#foreach($item in $!{columnDatas})
	<result column="$!item.columnName" property="$!item.formatColumnName"/>
#end
</resultMap>
       
<!-- $!{tableName} table all fields -->
<sql id="Base_Column_List" >
	 $!{SQL.columnFields}
</sql>
   
   
<!-- 查询条件 -->
<sql id="Example_Where_Clause">
where 1=1
<trim  suffixOverrides="," >
#foreach($item in $!{columnDatas})
	#set($testStr = "condition."+$!item.formatColumnName + " != null and condition." + $!item.formatColumnName + " != ''")
	#if($!item.dataType == 'String')
		#set($testStr = $!testStr + " and condition." + $!item.formatColumnName + " != ''")
 	#end
<if test="$!testStr" >
	    and $!item.columnName =  #{condition.$!item.formatColumnName}
	</if>
#end
</trim>
</sql>
   

<!-- 插入记录 -->
<insert id="add" parameterType="Object" >
#if  ($keyType =='02')
<selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="dId">
	SELECT LAST_INSERT_ID()
  </selectKey>
#end
  $!{SQL.insert}
</insert>

<!-- 根据id，修改记录-->  
 <update id="update" parameterType="Object" >
  $!{SQL.update}
 </update>
 
 <!-- 修改记录，只修改只不为空的字段 -->
<update id="updateBySelective" parameterType="Object" >
	$!{SQL.updateSelective}
</update>

<!-- 删除记录 -->
<delete id="delete" parameterType="Object">
	$!{SQL.delete}
</delete>
 
<!-- 根据id查询 ${codeName} -->
<select id="queryById"  resultMap="BaseResultMap" parameterType="Object">
	$!{SQL.selectById}
</select> 
#foreach($param in ${daoMethods.keySet()})
<select id="$param" resultType="java.util.HashMap" parameterType="Object">
	${daoMethods.get($param)}
	<if test="currentPage !=null  and pageSize!= null  ">
		 limit ${currentPage},${pageSize}
	</if>
</select> 
<select id="count$param" resultType="java.lang.Integer" parameterType="Object">
	select count(1) from (
	${daoMethods.get($param)}
	) $param
</select>
#end  
</mapper>
